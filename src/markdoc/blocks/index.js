import { Callout } from './Callout';
import { Tabs, Tab } from './Tabs';

const components = {
    // Must match 'render' property of schema
    Callout,
    Tab,
    Tabs
};

export default components;